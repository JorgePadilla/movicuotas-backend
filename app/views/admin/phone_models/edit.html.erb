<% content_for :title, "Editar #{@phone_model.brand} #{@phone_model.model} - Admin - MOVICUOTAS" %>

<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
  <!-- Page header -->
  <div class="mb-6">
    <%= link_to "← Volver a Catálogo", admin_phone_models_path, class: "text-[#125282] hover:underline text-sm font-medium" %>
    <h1 class="text-2xl lg:text-3xl font-bold text-[#125282] mt-2">Editar Teléfono</h1>
    <p class="text-gray-600 mt-2"><%= @phone_model.brand %> <%= @phone_model.model %></p>
  </div>

  <!-- Form -->
  <div class="bg-white rounded-xl shadow-md border border-gray-200 p-8">
    <%= render "form" %>
  </div>
</div>
