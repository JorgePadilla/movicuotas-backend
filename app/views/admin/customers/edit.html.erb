<% content_for :title, "Editar Cliente - Admin - MOVICUOTAS" %>

<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- Page header -->
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-[#125282]">Editar Cliente</h1>
    <p class="text-gray-600 mt-2"><%= @customer.full_name %> (<%= @customer.identification_number %>)</p>
  </div>

  <!-- Form -->
  <div class="bg-white rounded-xl shadow-md border border-gray-200 p-8">
    <%= render "form" %>
  </div>
</div>
