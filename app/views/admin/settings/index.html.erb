<% content_for :title, "Configuración - Admin - MOVICUOTAS" %>

<div class="max-w-4xl mx-auto px-3 sm:px-4 lg:px-6 py-4">
  <%= render Admin::BreadcrumbComponent.new(items: [
    { name: "Configuración" }
  ]) %>

  <!-- Page header -->
  <div class="mb-6 sm:mb-8">
    <h1 class="text-2xl sm:text-3xl font-bold text-[#125282]">Configuración del Sistema</h1>
    <p class="text-gray-600 mt-1 sm:mt-2 text-sm sm:text-base">Administrar configuraciones generales del sistema</p>
  </div>

  <!-- Support Phone Setting -->
  <div class="bg-white rounded-xl shadow-md border border-gray-200 p-4 sm:p-6">
    <div class="flex items-center mb-4">
      <svg class="h-6 w-6 text-[#125282] mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
      </svg>
      <h2 class="text-lg sm:text-xl font-semibold text-gray-900">Teléfono de Soporte</h2>
    </div>
    <p class="text-gray-600 text-sm mb-4">Este número se muestra en la aplicación móvil para que los clientes puedan llamar para consultas sobre pagos o bloqueo de dispositivos.</p>

    <%= form_with url: admin_settings_path, method: :patch, local: true, class: "space-y-4" do |f| %>
      <div>
        <label for="support_phone_number" class="block text-sm font-medium text-gray-700 mb-1">Número de Teléfono</label>
        <input
          type="tel"
          name="support_phone_number"
          id="support_phone_number"
          value="<%= @support_phone_number %>"
          placeholder="Ej: 97902401"
          class="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#125282] focus:border-[#125282] text-gray-900"
        >
      </div>
      <div>
        <label for="support_schedule" class="block text-sm font-medium text-gray-700 mb-1">Horario de Atención</label>
        <textarea
          name="support_schedule"
          id="support_schedule"
          rows="3"
          placeholder="Ej: Horario de atención:&#10;Lunes a Viernes 8:00 AM - 5:00 PM&#10;Sábado 8:00 AM - 12:00 PM"
          class="w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#125282] focus:border-[#125282] text-gray-900"
        ><%= @support_schedule %></textarea>
        <p class="mt-1 text-xs text-gray-500">Este texto se muestra en la pantalla de soporte de la app móvil. Use saltos de línea para separar los horarios.</p>
      </div>
      <div>
        <button type="submit" class="bg-[#125282] hover:bg-[#0f4670] text-white px-6 py-2.5 rounded-lg font-medium transition-colors">
          Guardar
        </button>
      </div>
    <% end %>
  </div>
</div>
