<% content_for :title, "Análisis por Sucursal - Admin - MOVICUOTAS" %>

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <!-- Page header -->
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-[#125282]">Análisis por Sucursal</h1>
    <p class="text-gray-600 mt-2">Desempeño, estadísticas y comparativas de cada sucursal</p>
  </div>

  <!-- Branch selector -->
  <div class="bg-white rounded-xl shadow-md border border-gray-200 p-6 mb-8">
    <label class="block text-sm font-medium text-gray-700 mb-3">Seleccionar Sucursal:</label>
    <div class="flex gap-2 flex-wrap">
      <%= link_to "Todas las Sucursales", branch_analytics_admin_reports_path, class: "#{@selected_branch.blank? ? 'bg-[#125282] text-white' : 'bg-white border border-[#125282] text-[#125282]'} px-4 py-2 rounded-lg font-medium text-sm transition-colors" %>
      <% @branches.each do |branch| %>
        <%= link_to "Sucursal #{branch}", branch_analytics_admin_reports_path(branch: branch), class: "#{@selected_branch.to_s == branch.to_s ? 'bg-[#125282] text-white' : 'bg-white border border-gray-300 text-gray-700'} px-4 py-2 rounded-lg font-medium text-sm transition-colors hover:border-[#125282]" %>
      <% end %>
    </div>
  </div>

  <!-- Branch Details (if selected) -->
  <% if @selected_branch.present? && @branch_loans.any? %>
    <!-- Branch Statistics -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
      <div class="bg-white rounded-xl shadow-md border border-gray-200 p-6">
        <h3 class="text-lg font-semibold text-[#125282]">Total de Préstamos</h3>
        <p class="text-3xl font-bold text-gray-800 mt-2"><%= @branch_loan_count %></p>
        <div class="mt-4 space-y-1 text-sm">
          <div class="flex justify-between">
            <span class="text-gray-600">Activos</span>
            <span class="font-medium text-green-600"><%= @branch_active_loans %></span>
          </div>
          <div class="flex justify-between">
            <span class="text-gray-600">Completados</span>
            <span class="font-medium text-blue-600"><%= @branch_completed_loans %></span>
          </div>
          <div class="flex justify-between">
            <span class="text-gray-600">En Mora</span>
            <span class="font-medium text-red-600"><%= @branch_overdue_loans %></span>
          </div>
        </div>
      </div>

      <div class="bg-white rounded-xl shadow-md border border-gray-200 p-6">
        <h3 class="text-lg font-semibold text-[#125282]">Valor de Cartera</h3>
        <p class="text-3xl font-bold text-gray-800 mt-2">L. <%= number_with_delimiter(@branch_total_value, delimiter: ',') %></p>
        <div class="mt-4 space-y-1 text-sm">
          <div class="flex justify-between">
            <span class="text-gray-600">Pagado</span>
            <span class="font-medium text-green-600">L. <%= number_with_delimiter(@branch_total_paid, delimiter: ',') %></span>
          </div>
          <div class="flex justify-between">
            <span class="text-gray-600">Pendiente</span>
            <span class="font-medium text-orange-600">L. <%= number_with_delimiter(@branch_remaining_balance, delimiter: ',') %></span>
          </div>
        </div>
      </div>

      <div class="bg-white rounded-xl shadow-md border border-gray-200 p-6">
        <h3 class="text-lg font-semibold text-[#125282]">Tasa de Cobranza</h3>
        <% cobranza_rate = @branch_total_value > 0 ? ((@branch_total_paid / @branch_total_value) * 100).round(2) : 0 %>
        <p class="text-3xl font-bold text-gray-800 mt-2"><%= cobranza_rate %>%</p>
        <div class="mt-4 text-sm text-gray-600">
          Porcentaje de monto cobrado del total generado
        </div>
      </div>

      <div class="bg-white rounded-xl shadow-md border border-gray-200 p-6">
        <h3 class="text-lg font-semibold text-[#125282]">Métodos de Pago</h3>
        <div class="mt-4 space-y-2 text-sm">
          <% @branch_payment_methods.each do |method, amount| %>
            <div class="flex justify-between">
              <span class="text-gray-600"><%= method.titleize %></span>
              <span class="font-medium">L. <%= number_with_delimiter(amount, delimiter: ',') %></span>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Top Customers in Branch -->
    <div class="bg-white rounded-xl shadow-md border border-gray-200 p-6 mb-8">
      <h2 class="text-xl font-semibold text-[#125282] mb-6">👥 Principales Clientes en Sucursal <%= @selected_branch %></h2>
      <% if @top_customers_in_branch.any? %>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Cliente</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Identidad</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Préstamos</th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Teléfono</th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% @top_customers_in_branch.each do |customer| %>
                <tr class="hover:bg-gray-50">
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="font-medium text-gray-900"><%= customer.full_name %></span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="text-sm text-gray-600"><%= customer.identification_number %></span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="text-sm font-medium text-gray-800"><%= customer.loan_count %></span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="text-sm text-gray-600"><%= customer.phone || "—" %></span>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <p class="text-gray-500 text-center py-8">No hay clientes en esta sucursal</p>
      <% end %>
    </div>
  <% elsif @selected_branch.blank? %>
    <!-- Overall View -->
    <div class="bg-blue-50 rounded-xl border border-blue-200 p-6 mb-8">
      <p class="text-blue-800">
        <strong>Sugerencia:</strong> Selecciona una sucursal específica arriba para ver análisis detallados de desempeño, clientes principales y tendencias.
      </p>
    </div>
  <% elsif @selected_branch.present? && @branch_loans.empty? %>
    <div class="bg-yellow-50 rounded-xl border border-yellow-200 p-6 mb-8">
      <p class="text-yellow-800">
        <strong>Sin datos:</strong> La sucursal <%= @selected_branch %> no tiene préstamos registrados.
      </p>
    </div>
  <% end %>

  <!-- Footer navigation -->
  <div class="flex justify-between items-center mt-8">
    <div>
      <%= link_to "← Volver a Reportes", admin_reports_path, class: "text-[#125282] hover:underline font-medium" %>
    </div>
    <div>
      <%= link_to "← Volver al Dashboard", admin_dashboard_path, class: "text-[#125282] hover:underline font-medium" %>
    </div>
  </div>
</div>
