<% content_for :title, "Registrar Pago - Admin - MOVICUOTAS" %>

<div class="max-w-2xl mx-auto px-3 sm:px-4 lg:px-6 py-4">
  <%= render Admin::BreadcrumbComponent.new(items: [
    { name: "Pagos", path: admin_payments_path },
    { name: "Registrar Pago" }
  ]) %>

  <!-- Page header -->
  <div class="mb-6 sm:mb-8">
    <h1 class="text-2xl sm:text-3xl font-bold text-[#125282]">Registrar Pago</h1>
    <p class="text-gray-600 mt-1 sm:mt-2 text-sm sm:text-base">Registra un pago y asócialo a una cuota del préstamo.</p>
  </div>

  <!-- Form -->
  <div class="bg-white rounded-xl shadow-md border border-gray-200 p-6 sm:p-8">
    <%= render "form" %>
  </div>
</div>
