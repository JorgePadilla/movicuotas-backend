<% content_for :title, "¡Gracias por tu Confianza! - MOVICUOTAS" %>

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <%# Progress Indicator %>
  <div class="mb-8 sm:mb-12 overflow-x-auto">
    <div class="flex items-center justify-between gap-1 sm:gap-4 mb-4 min-w-min sm:min-w-0">
      <div class="flex items-center gap-2 sm:gap-3 flex-shrink-0">
        <div class="w-7 h-7 sm:w-8 sm:h-8 rounded-full bg-[#125282] text-white flex items-center justify-center font-bold text-xs sm:text-sm">16</div>
        <span class="font-medium text-gray-500 text-xs sm:text-base">QR MDM</span>
      </div>
      <div class="h-1 flex-shrink-0 w-6 sm:flex-1 sm:mx-2 sm:mx-4 bg-[#125282]"></div>
      <div class="flex items-center gap-2 sm:gap-3 flex-shrink-0">
        <div class="w-7 h-7 sm:w-8 sm:h-8 rounded-full bg-[#125282] text-white flex items-center justify-center font-bold text-xs sm:text-sm">17</div>
        <span class="font-medium text-gray-500 text-xs sm:text-base">Activación</span>
      </div>
      <div class="h-1 flex-shrink-0 w-6 sm:flex-1 sm:mx-2 sm:mx-4 bg-[#125282]"></div>
      <div class="flex items-center gap-2 sm:gap-3 flex-shrink-0">
        <div class="w-7 h-7 sm:w-8 sm:h-8 rounded-full bg-[#125282] text-white flex items-center justify-center font-bold text-xs sm:text-sm">18</div>
        <span class="font-medium text-gray-700 text-xs sm:text-base">Gracias</span>
      </div>
    </div>
    <p class="text-center text-xs sm:text-sm text-gray-500 mt-2">Paso 18 de 18 - Flujo de Venta Completado</p>
  </div>

  <%# Thank You Card %>
  <div class="bg-white shadow-xl rounded-2xl overflow-hidden border-4 border-[#125282]">
    <div class="bg-gradient-to-r from-[#125282] to-[#0f4670] p-8 text-center">
      <div class="inline-flex items-center justify-center w-24 h-24 bg-white/20 rounded-full mb-6">
        <svg class="w-12 h-12 text-white" fill="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
          <path d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"/>
        </svg>
      </div>
      <h1 class="text-4xl font-bold text-white mb-4">¡Gracias por tu Confianza!</h1>
      <p class="text-xl text-blue-100 mb-2">Es un placer servirte. Estamos aquí para apoyarte en cada paso.</p>
      <p class="text-lg text-blue-100 font-medium">MOVICUOTAS - Tu Crédito, Tu Móvil</p>
    </div>

    <div class="p-8">
      <%# Contract Summary %>
      <div class="bg-gray-50 rounded-xl p-6 mb-8">
        <h2 class="text-xl font-bold text-gray-900 mb-4">Resumen del Crédito</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <p class="text-sm font-medium text-gray-500">Cliente</p>
            <p class="text-lg font-semibold text-[#125282]"><%= @customer.full_name %></p>
            <p class="text-gray-600"><%= @customer.identification_number %></p>
          </div>
          <div>
            <p class="text-sm font-medium text-gray-500">Contrato</p>
            <p class="text-lg font-semibold text-[#125282]"><%= @loan.contract_number %></p>
            <p class="text-gray-600">Firmado: <%= l(@contract.signed_at, format: :long) if @contract.signed_at %></p>
          </div>
          <div>
            <p class="text-sm font-medium text-gray-500">Dispositivo</p>
            <% if @device.present? %>
              <p class="text-lg font-semibold text-[#125282]"><%= @device.brand %> <%= @device.model %></p>
              <p class="text-gray-600">IMEI: <%= @device.imei %></p>
            <% else %>
              <p class="text-lg font-semibold text-gray-400">No seleccionado</p>
              <p class="text-gray-400">Por favor, completa los datos del dispositivo en el paso anterior.</p>
            <% end %>
          </div>
          <div>
            <p class="text-sm font-medium text-gray-500">Próximo Pago</p>
            <p class="text-lg font-semibold text-[#125282]">
              <% next_installment = @loan.next_installment %>
              <% if next_installment %>
                L. <%= number_with_delimiter(next_installment.amount.ceil, delimiter: ",") %>
                <span class="block text-sm font-normal text-gray-600">
                  <%= l(next_installment.due_date, format: :long) %>
                </span>
              <% else %>
                <span class="text-gray-500">No hay pagos pendientes</span>
              <% end %>
            </p>
          </div>
        </div>
      </div>

      <%# Action Button: Download Contract %>
      <div class="mb-8">
        <%= link_to download_vendor_contract_path(@contract),
                    class: "group flex flex-col items-center justify-center p-8 bg-[#125282] hover:bg-[#0f4670] rounded-xl transition-colors duration-200 text-white text-center" do %>
          <svg class="w-12 h-12 mb-4 group-hover:scale-110 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
          <h3 class="text-xl font-bold mb-2">Descargar Contrato</h3>
          <p class="text-blue-100">Guarda una copia del contrato firmado en tu dispositivo.</p>
        <% end %>
      </div>

      <%# Important Notes %>
      <div class="bg-green-50 border-l-4 border-green-400 p-6 mb-8">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
          </div>
          <div class="ml-3">
            <h3 class="text-sm font-medium text-green-800">¡Venta completada exitosamente!</h3>
            <div class="mt-2 text-sm text-green-700">
              <ul class="list-disc pl-5 space-y-1">
                <li>El dispositivo ha sido configurado y está listo para entregar al cliente.</li>
                <li>Recuerda al cliente las fechas de pago quincenales para evitar mora.</li>
                <li>Descarga una copia del contrato firmado para tus registros.</li>
              </ul>
            </div>
          </div>
        </div>
      </div>

      <%# Navigation %>
      <div class="flex flex-col md:flex-row justify-center items-center pt-8 border-t border-gray-200">
        <%= link_to "Nueva Búsqueda de Cliente", vendor_customer_search_path,
                    class: "bg-gray-800 text-white font-semibold py-3 px-8 rounded-lg hover:bg-gray-900 transition-colors duration-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-800" %>
      </div>
    </div>
  </div>
</div>