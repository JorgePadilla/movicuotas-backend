<%# app/views/vendor/contracts/signature.html.erb - Step 14: Digital Signature %>
<% content_for :title, "Firma Digital - Movicuotas" %>
<% content_for :head do %>
  <%= javascript_import_module_tag "controllers/signature_controller" %>
<% end %>

<div class="max-w-7xl mx-auto px-2 sm:px-3 lg:px-4 py-4">
  <!-- Header -->
  <div class="mb-4 sm:mb-8">
    <h1 class="text-xl sm:text-3xl font-bold text-[#125282]">Firma Digital</h1>
    <p class="text-gray-600 mt-1 text-xs sm:text-base">Capture la firma del cliente en el área designada.</p>

    <div class="mt-3 sm:mt-4 p-2.5 sm:p-4 bg-blue-50 border border-blue-200 rounded-lg">
      <div class="flex items-center justify-between">
        <div class="flex items-center gap-2 sm:gap-3 min-w-0">
          <svg class="h-5 w-5 text-blue-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"/>
          </svg>
          <div class="min-w-0">
            <p class="font-medium text-blue-800 text-xs sm:text-base truncate"><%= @contract.loan.customer.full_name %></p>
            <p class="text-xs text-blue-600 truncate"><%= @contract.loan.contract_number %></p>
          </div>
        </div>
        <div class="text-right flex-shrink-0 ml-2">
          <span class="inline-flex items-center px-2 py-1 bg-blue-100 text-blue-700 rounded-full text-xs font-medium">14/19</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Signature Canvas Container -->
  <div class="bg-white rounded-lg sm:rounded-xl shadow-lg overflow-hidden mb-4 sm:mb-8 border border-gray-200">
    <!-- Canvas Header -->
    <div class="bg-[#125282] text-white p-2.5 sm:p-4">
      <h2 class="text-sm sm:text-xl font-bold text-center">FIRMA DEL CLIENTE</h2>
      <p class="text-center text-xs opacity-90 mt-0.5 sm:mt-1">Firme con el dedo en el área de abajo</p>
    </div>

    <!-- Canvas Area -->
    <div class="p-2.5 sm:p-4 md:p-6">
      <div data-controller="signature"
           data-signature-save-url="<%= save_signature_vendor_contract_path(@contract) %>"
           data-action="
             mousedown->signature#startDrawing
             mousemove->signature#draw
             mouseup->signature#stopDrawing
             mouseleave->signature#stopDrawing
             touchstart->signature#startDrawing
             touchmove->signature#draw
             touchend->signature#stopDrawing
           ">

        <!-- Canvas -->
        <div class="mb-3 sm:mb-6">
          <canvas id="signatureCanvas"
                  data-signature-target="canvas"
                  class="w-full border-2 border-[#125282] rounded-lg bg-white touch-none"
                  style="height: 180px;"
                  width="800"
                  height="400">
            Su navegador no soporta el elemento canvas.
          </canvas>
          <p class="text-center text-xs text-gray-500 mt-1 sm:hidden">Firme con el dedo aquí ↑</p>
        </div>

        <!-- Instructions - Collapsible on mobile -->
        <details class="mb-3 sm:mb-6 bg-gray-50 rounded-lg border border-gray-200">
          <summary class="p-2.5 sm:p-4 cursor-pointer font-medium text-gray-700 text-xs sm:text-base flex items-center">
            <svg class="h-4 w-4 mr-2 text-gray-500 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            Instrucciones
          </summary>
          <ul class="list-disc pl-6 sm:pl-8 pb-2.5 sm:pb-4 pr-2.5 sm:pr-4 text-gray-600 space-y-1 text-xs sm:text-sm">
            <li>Firme con el dedo o mouse en el área blanca.</li>
            <li>La firma debe ser clara y legible.</li>
            <li>Use "Limpiar" para empezar de nuevo.</li>
          </ul>
        </details>

        <!-- Action Buttons - Mobile optimized -->
        <div class="space-y-2 sm:space-y-0 sm:flex sm:flex-wrap sm:gap-3 sm:justify-between sm:items-center">
          <!-- Primary action first on mobile -->
          <button type="button"
                  data-action="signature#save"
                  class="w-full sm:w-auto px-4 py-2.5 bg-[#10b981] text-white rounded-lg hover:bg-green-700 transition-colors font-medium flex items-center justify-center text-sm order-1 sm:order-3">
            <svg class="h-4 w-4 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
            </svg>
            Guardar Firma
          </button>

          <!-- Secondary actions -->
          <div class="flex gap-2 order-2 sm:order-1">
            <button type="button"
                    data-action="signature#clear"
                    class="flex-1 sm:flex-none px-3 sm:px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors flex items-center justify-center text-xs sm:text-sm">
              <svg class="h-4 w-4 mr-1.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
              </svg>
              Limpiar
            </button>

            <button type="button"
                    data-action="signature#undo"
                    class="flex-1 sm:flex-none px-3 sm:px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors flex items-center justify-center text-xs sm:text-sm">
              <svg class="h-4 w-4 mr-1.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h10a8 8 0 018 8v2M3 10l6 6m-6-6l6-6"/>
              </svg>
              Deshacer
            </button>
          </div>

          <%= link_to vendor_contract_path(@contract), class: "w-full sm:w-auto px-4 py-2 border border-gray-300 text-gray-700 rounded-lg hover:bg-gray-50 transition-colors text-xs sm:text-sm text-center order-3 sm:order-2" do %>
            ← Volver
          <% end %>
        </div>

        <!-- Hidden form for submission -->
        <%= form_with url: save_signature_vendor_contract_path(@contract), method: :post, id: "signatureForm", class: "hidden" do |f| %>
          <%= f.hidden_field :signature_data, id: "signatureData", data: { signature_target: "data" } %>
        <% end %>

        <!-- Status messages -->
        <div class="mt-4">
          <div data-signature-target="status" class="hidden p-3 rounded-lg text-sm"></div>
        </div>
      </div>
    </div>
  </div>

  <!-- Signature Preview (if already signed) -->
  <% if @contract.signature_image.attached? %>
    <div class="bg-white rounded-lg sm:rounded-xl shadow-lg overflow-hidden mb-4 sm:mb-8 border border-gray-200">
      <div class="bg-green-50 text-green-800 p-2.5 sm:p-4">
        <h3 class="font-bold text-sm sm:text-lg flex items-center">
          <svg class="h-5 w-5 mr-2 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          Firma Guardada
        </h3>
      </div>
      <div class="p-3 sm:p-6">
        <div class="flex items-center gap-3 sm:gap-6 mb-4">
          <div class="border border-gray-300 rounded-lg p-2 sm:p-4 bg-white flex-shrink-0">
            <%= image_tag @contract.signature_image, class: "max-h-16 sm:max-h-32" %>
          </div>
          <div class="text-xs text-gray-500">
            <p><%= @contract.signed_at&.strftime("%d/%m/%Y %H:%M") %></p>
            <p><%= @contract.signed_by_name %></p>
          </div>
        </div>
        <%= link_to "Continuar a Prima →", vendor_contract_down_payment_path(@contract), class: "w-full sm:w-auto inline-block px-4 py-2.5 bg-[#125282] text-white rounded-lg hover:bg-[#0f4670] transition-colors font-medium text-sm text-center" %>
      </div>
    </div>
  <% end %>

  <!-- Legal Disclaimer - Collapsible on mobile -->
  <details class="bg-gray-50 border border-gray-200 rounded-lg sm:rounded-xl mb-4 sm:mb-8">
    <summary class="p-3 sm:p-4 cursor-pointer font-medium text-gray-700 text-xs sm:text-base">
      Validez Legal de la Firma Digital
    </summary>
    <div class="px-3 pb-3 sm:px-6 sm:pb-6 text-gray-600 text-xs space-y-2">
      <p>Según la <strong>Ley de Firma Electrónica de Honduras</strong> (Decreto No. 101-2013), esta firma digital tiene validez jurídica.</p>
      <p class="italic text-xs text-gray-500">MOVICUOTAS garantiza la seguridad mediante encriptación y almacenamiento seguro.</p>
    </div>
  </details>

  <!-- Progress Indicator - Simplified for mobile -->
  <div class="mt-4 sm:mt-12">
    <!-- Mobile: Simple indicator -->
    <div class="sm:hidden text-center">
      <div class="inline-flex items-center gap-2 bg-[#125282] text-white px-4 py-2 rounded-full">
        <span class="font-bold">14</span>
        <span class="text-sm">Firma</span>
      </div>
      <p class="text-xs text-gray-500 mt-2">Paso 14 de 18</p>
    </div>

    <!-- Desktop: Full progress indicator -->
    <div class="hidden sm:block">
      <div class="flex items-center justify-between gap-4 mb-4">
        <div class="flex items-center gap-3 flex-shrink-0">
          <div class="w-8 h-8 rounded-full bg-[#125282] text-white flex items-center justify-center font-bold text-sm">13</div>
          <span class="font-medium text-gray-500">Contrato</span>
        </div>
        <div class="h-1 flex-1 mx-4 bg-[#125282]"></div>
        <div class="flex items-center gap-3 flex-shrink-0">
          <div class="w-8 h-8 rounded-full bg-[#125282] text-white flex items-center justify-center font-bold text-sm">14</div>
          <span class="font-medium text-gray-700">Firma</span>
        </div>
        <div class="h-1 flex-1 mx-4 bg-gray-200"></div>
        <div class="flex items-center gap-3 flex-shrink-0">
          <div class="w-8 h-8 rounded-full bg-gray-300 text-gray-600 flex items-center justify-center font-bold text-sm">15</div>
          <span class="font-medium text-gray-500">Prima</span>
        </div>
        <div class="h-1 flex-1 mx-4 bg-gray-200"></div>
        <div class="flex items-center gap-3 flex-shrink-0">
          <div class="w-8 h-8 rounded-full bg-gray-300 text-gray-600 flex items-center justify-center font-bold text-sm">16</div>
          <span class="font-medium text-gray-500">Crédito</span>
        </div>
      </div>
      <p class="text-center text-sm text-gray-500 mt-2">Paso 14 de 18 - Flujo de Venta</p>
    </div>
  </div>
</div>