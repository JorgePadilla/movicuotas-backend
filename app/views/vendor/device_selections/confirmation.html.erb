<%# Step 11: Confirmación (Purchase Summary) %>
<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8 text-center">
    <h1 class="text-3xl font-bold text-[#125282]">Confirmación de Compra</h1>
    <p class="mt-2 text-gray-600">
      Revise los detalles del teléfono seleccionado antes de proceder al cálculo de pagos.
    </p>
  </div>

  <div class="bg-white rounded-xl border border-gray-200 shadow-sm overflow-hidden mb-8">
    <div class="p-6 border-b border-gray-200 bg-gray-50">
      <h2 class="text-xl font-semibold text-gray-800">Resumen de la Solicitud</h2>
      <p class="text-sm text-gray-600 mt-1">
        Solicitud: <span class="font-medium"><%= @credit_application.application_number %></span> |
        Cliente: <span class="font-medium"><%= @credit_application.customer.full_name %></span>
      </p>
    </div>

    <div class="p-6">
      <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-6">
        <%# Selected phone details %>
        <div class="flex-1">
          <h3 class="text-lg font-medium text-gray-900 mb-4">Teléfono Seleccionado</h3>
          <div class="flex items-start space-x-4">
            <% if @credit_application.selected_phone_model.image_url.present? %>
              <div class="w-24 h-24 bg-gray-100 rounded-lg flex items-center justify-center flex-shrink-0">
                <img src="<%= @credit_application.selected_phone_model.image_url %>" alt="<%= @credit_application.selected_phone_model.brand %>" class="max-w-full max-h-full object-contain">
              </div>
            <% end %>
            <div>
              <h4 class="text-xl font-bold text-gray-900">
                <%= @credit_application.selected_phone_model.brand %> <%= @credit_application.selected_phone_model.model %>
              </h4>
              <p class="text-gray-600 mt-1">
                <%= @credit_application.selected_phone_model.storage %>
              </p>
              <div class="mt-3">
                <div class="text-3xl font-bold text-[#1f2937]">
                  L. <%= number_to_currency(@credit_application.selected_phone_model.price, unit: "", delimiter: ",") %>
                </div>
                <p class="text-sm text-gray-500">Precio total del teléfono</p>
              </div>
            </div>
          </div>

          <%# Device-specific details (IMEI & Color) %>
          <div class="mt-8 pt-6 border-t border-gray-200">
            <h4 class="text-lg font-medium text-gray-900 mb-3">Datos del Dispositivo Físico</h4>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div>
                <p class="text-sm text-gray-600">IMEI</p>
                <p class="font-mono text-gray-900 bg-gray-50 p-2 rounded border"><%= @credit_application.selected_imei %></p>
              </div>
              <div>
                <p class="text-sm text-gray-600">Color</p>
                <p class="font-medium text-gray-900"><%= @credit_application.selected_color %></p>
              </div>
            </div>
          </div>
        </div>

        <%# Next step info %>
        <div class="md:w-64 flex-shrink-0">
          <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
            <h4 class="text-sm font-medium text-blue-800 mb-2">Próximo Paso</h4>
            <p class="text-sm text-blue-700">
              Proceder al cálculo de pagos quincenales con opciones de enganche y plazo.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <%# Navigation %>
  <div class="flex items-center justify-between pt-6 border-t border-gray-200">
    <div>
      <%= link_to "← Editar selección", vendor_device_selection_path(@credit_application), class: "text-sm text-[#125282] hover:underline" %>
    </div>
    <div class="flex space-x-4">
      <%= link_to "Cancelar", vendor_customer_search_path, class: "px-4 py-2 border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50" %>
      <%= link_to "Continuar a Calculadora →", new_vendor_payment_calculator_path(phone_price: @credit_application.selected_phone_model.price, approved_amount: @max_financing_amount, credit_application_id: @credit_application.id), class: "px-6 py-2 bg-[#125282] text-white font-medium rounded-md hover:bg-[#125282]/90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#125282] cursor-pointer" %>
    </div>
  </div>

  <div class="mt-8 text-center text-sm text-gray-500">
    <p>Paso 11 de 18 - Confirmación de Compra</p>
    <div class="mt-2 flex justify-center space-x-1">
      <% (1..19).each do |step| %>
        <div class="w-2 h-2 rounded-full <%= step == 11 ? 'bg-[#125282]' : 'bg-gray-300' %>"></div>
      <% end %>
    </div>
  </div>
</div>