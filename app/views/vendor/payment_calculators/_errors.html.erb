<%# Errors Partial for Payment Calculator (Step 12) %>
<%# This partial is rendered via Turbo Stream when calculation fails %>

<div class="bg-red-50 border border-red-200 rounded-lg p-3 sm:p-6 mb-6 sm:mb-8">
  <div class="flex gap-3 sm:gap-0">
    <div class="flex-shrink-0">
      <svg class="h-4 sm:h-5 w-4 sm:w-5 text-red-600" fill="currentColor" viewBox="0 0 20 20">
        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
      </svg>
    </div>
    <div class="sm:ml-3">
      <h3 class="text-xs sm:text-sm font-medium text-red-800">Error en el cálculo</h3>
      <div class="mt-2 text-xs sm:text-sm text-red-700">
        <ul class="list-disc pl-5 space-y-1">
          <% errors.each do |error| %>
            <li><%= error %></li>
          <% end %>
        </ul>
      </div>
      <div class="mt-3 sm:mt-4">
        <p class="text-xs sm:text-sm text-red-700">
          Por favor, verifica los valores ingresados e intenta nuevamente.
        </p>
      </div>
    </div>
  </div>
</div>