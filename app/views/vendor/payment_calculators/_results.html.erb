<%# Results Partial for Payment Calculator (Step 12) %>
<%# This partial is rendered via Turbo Stream when calculator updates %>

<div class="bg-white shadow-lg rounded-lg p-8 border-t-4 border-green-500">
  <h2 class="text-2xl font-bold text-gray-900 mb-6">Resultados del Cálculo</h2>

  <%# Main Installment Amount - Large Display %>
  <div class="text-center mb-8">
    <p class="text-sm font-medium text-gray-500 uppercase tracking-wider">Cuota Quincenal</p>
    <p class="text-5xl font-bold text-green-600 mt-2">L. <%= number_with_delimiter(result[:installment_amount].round(2), delimiter: ",") %></p>
    <p class="text-gray-600 mt-2">Cada 15 días durante <%= result[:number_of_installments] %> pagos</p>
  </div>

  <%# Calculation Summary %>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-6 mb-8">
    <div class="bg-gray-50 p-4 rounded-lg">
      <p class="text-sm font-medium text-gray-500">Pago Inicial</p>
      <p class="text-xl font-semibold text-[#125282]">L. <%= number_with_delimiter(result[:down_payment_amount].round(2), delimiter: ",") %></p>
      <p class="text-sm text-gray-600 mt-1"><%= result[:down_payment_percentage] %>% del total</p>
    </div>

    <div class="bg-gray-50 p-4 rounded-lg">
      <p class="text-sm font-medium text-gray-500">Monto Financiado</p>
      <p class="text-xl font-semibold text-gray-900">L. <%= number_with_delimiter(result[:financed_amount].round(2), delimiter: ",") %></p>
      <p class="text-sm text-gray-600 mt-1">A pagar en cuotas</p>
    </div>

    <div class="bg-gray-50 p-4 rounded-lg">
      <p class="text-sm font-medium text-gray-500">Tasa Quincenal</p>
      <p class="text-xl font-semibold text-orange-600"><%= result[:bi_weekly_rate_percentage] %>%</p>
      <p class="text-sm text-gray-600 mt-1">Bi-semanal</p>
    </div>

    <div class="bg-gray-50 p-4 rounded-lg">
      <p class="text-sm font-medium text-gray-500">Interés Total</p>
      <p class="text-xl font-semibold text-orange-600">L. <%= number_with_delimiter(result[:total_interest].round(2), delimiter: ",") %></p>
      <p class="text-sm text-gray-600 mt-1">Costo financiero</p>
    </div>

    <div class="bg-gray-50 p-4 rounded-lg">
      <p class="text-sm font-medium text-gray-500">Edad Cliente</p>
      <p class="text-xl font-semibold text-[#125282]"><%= result[:age] %> años</p>
      <p class="text-sm text-gray-600 mt-1">
        <% if result[:age_group] == 1 %>
          Grupo 1 (21-49 años)
        <% elsif result[:age_group] == 2 %>
          Grupo 2 (50-60 años)
        <% end %>
      </p>
    </div>

    <div class="bg-gray-50 p-4 rounded-lg">
      <p class="text-sm font-medium text-gray-500">Pago Total</p>
      <p class="text-xl font-semibold text-gray-900">L. <%= number_with_delimiter(result[:total_payment].round(2), delimiter: ",") %></p>
      <p class="text-sm text-gray-600 mt-1">Incluye precio + interés</p>
    </div>
  </div>

  <%# Installment Schedule (First 3 installments) %>
  <div class="border border-gray-200 rounded-lg p-6">
    <h3 class="text-lg font-medium text-gray-900 mb-4">Calendario de Pagos (Primeras 3 cuotas)</h3>
    <div class="space-y-4">
      <% result[:installments].first(3).each do |installment| %>
        <div class="flex items-center justify-between py-3 px-4 bg-gray-50 rounded-lg">
          <div>
            <p class="font-medium text-gray-900">Cuota <%= installment[:installment_number] %></p>
            <p class="text-sm text-gray-600"><%= installment[:due_date].strftime("%d/%m/%Y") %></p>
          </div>
          <div class="text-right">
            <p class="text-lg font-semibold text-[#125282]">L. <%= number_with_delimiter(installment[:amount].round(2), delimiter: ",") %></p>
            <p class="text-sm text-gray-600">Quincenal</p>
          </div>
        </div>
      <% end %>

      <% if result[:installments].size > 3 %>
        <div class="text-center pt-4 border-t border-gray-200">
          <p class="text-gray-600">
            + <%= result[:installments].size - 3 %> cuotas más
            <span class="text-gray-400">•</span>
            Finaliza: <%= result[:installments].last[:due_date].strftime("%d/%m/%Y") %>
          </p>
        </div>
      <% end %>
    </div>
  </div>

  <%# Success Message %>
  <div class="mt-8 p-4 bg-green-50 border border-green-200 rounded-lg">
    <div class="flex">
      <div class="flex-shrink-0">
        <svg class="h-5 w-5 text-green-600" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
        </svg>
      </div>
      <div class="ml-3">
        <p class="text-sm font-medium text-green-800">
          ¡Cálculo completado! Tu cuota quincenal es de <strong>L. <%= number_with_delimiter(result[:installment_amount].round(2), delimiter: ",") %></strong>.
        </p>
        <p class="text-sm text-green-700 mt-1">
          Puedes continuar al contrato o ajustar las opciones de pago para ver diferentes escenarios.
        </p>
      </div>
    </div>
  </div>
</div>